const root = require('app-root-path');

module.exports = {
  assets: `${root}/assets`,
  client: `${root}/client`,
  logs: `${root}/logs`,
  public: `${root}/public`,
  server: `${root}/server`,
  storage: `${root}/storage`,
  utils: `${root}/utils`
};
